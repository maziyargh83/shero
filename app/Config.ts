import { SHERO_PRODUCT } from "~/data/constants";
import type { configType } from "~/types";

export const CONFIG: configType = {
  imageUrlPrefix: "/images/",
  product: SHERO_PRODUCT,
};
export default CONFIG;
